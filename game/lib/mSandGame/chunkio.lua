local bytbin = require("lib.mBin.bytbin")
local chunk = require("lib.mSandGame.chunk")

local chunkio = {}

function chunkio.save(c, worldpath)
end

function chunkio.load(worldpath, cx, cy)
end

return chunkio